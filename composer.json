{
    "name": "idio/api-php",
    "version": "0.5.0",
    "autoload" : {
        "psr-0" : {"Idio": "lib"}
    },
    "require" : {
        "jakeasmith/http_build_url": "0.1.*",
        "ext-curl": "*",
        "ext-mbstring": "*"
    },
    "require-dev" : {
        "phpunit/phpunit": "3.7.*"
    },
    "suggest" : {
        "ext-http" : "The HTTP extension (version 1.x) is recommended. A compatible library is included if it is not."
    }
}
